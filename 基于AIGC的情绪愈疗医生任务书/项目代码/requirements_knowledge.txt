# 知识库管理工具依赖（低内存优化版 - 适合2GB内存）

# 向量化模型（超轻量级，约80MB）
sentence-transformers>=2.2.0

# 文本处理（使用更小的版本）
scikit-learn>=1.2.0
numpy<2.0.0  # 使用1.x版本以节省内存

# 文件读取
pypdf>=3.17.0  # PDF读取
python-docx>=1.1.0  # Word文档读取

# HTTP请求
requests>=2.28.0

# 优化：使用更轻的依赖
# jieba>=0.42.1  # 中文分词（可选）
